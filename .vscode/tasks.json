{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Docker: Run Development Server",
            "type": "shell",
            "command": "docker-compose up --build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}